В группе и канале должен быть строковый id
В сообщении:
    обязательный параметр - переменная fromWho типа User (юзер который отправил соо)

    обязательно одно из них:
    переменная group типа Group (группа в которой соо)
    переменная channel типа Channel (канал в котором соо)
    переменная userChat типа User (тут хранится юзер которому пришло это соо)
        (если соо в группе, то канал и юзерЧат = null и тд)





